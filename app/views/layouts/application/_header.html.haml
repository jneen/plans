%header
  #welcome
    - if current_account.logged_in?
      Welcome back, #{current_account.link}
      %a{href: url_for(controller: :sessions, action: :clear)} (Logout)
      %a{href: edit_account_path(current_account.login)} (Edit Account)
